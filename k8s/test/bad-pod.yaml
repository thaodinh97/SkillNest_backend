apiVersion: v1
kind: Pod
metadata:
  name: bad-privileged-pod
  labels:
    app: bad-pod
spec:
  containers:
    - name: nginx
      image: nginx
      securityContext:
        privileged: true # CỐ TÌNH VI PHẠM
